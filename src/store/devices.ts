
// Re-export everything from the refactored structure
export { createDeviceSlice } from './devices/slice';
export type { DeviceActions, DeviceSlice } from './devices/slice';
export type { DeviceActions as DeviceActionsType } from './devices/types';
